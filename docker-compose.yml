services:
  web:
    build: .
    ports:
      - "${WEB_PORT:-5050}:5050"
    environment:
      - FLASK_SECRET=${FLASK_SECRET:-dev-secret}
      - ROUND_TIME_LIMIT_SECONDS=${ROUND_TIME_LIMIT_SECONDS:-120}
    volumes:
      - .:/app
