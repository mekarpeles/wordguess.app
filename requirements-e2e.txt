-r requirements-dev.txt
# Unpinned: playwright's greenlet dependency needs a version with a
# prebuilt wheel for the local Python; an old pin can force a source
# build of greenlet that fails on newer Python versions (e.g. 3.14
# changed internal frame structures greenlet's C extension relies on).
playwright
pytest-playwright
